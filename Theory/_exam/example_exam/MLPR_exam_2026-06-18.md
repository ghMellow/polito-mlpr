# 01URTOV
# MACHINE LEARNING AND PATTERN RECOGNITION

## WRITTEN EXAM — Duration: 140 minutes — Appello 18 giugno 2026

Theory part [max 24 points]

**Theory - question 1 [12 points]**

Describe the common characteristics of generative models for classification, covering the following aspects:

- a. How generative models describe the relationship between features and class labels.
- b. The Maximum Likelihood training framework for generative models.
- c. How generative models can be employed to perform inference, both in binary and multiclass scenarios, explaining also the role of class prior probabilities.

Consider now the Gaussian (MVG) and Gaussian Mixture Model (GMM) approaches.

- e. Briefly explain the characteristics of tasks that can be addressed by these two models.
- f. Provide explicit expressions for the class-conditional likelihoods for these two models, and explain the form of the decision rules of MVG models.

Explain the Naive Bayes assumption and how it can be employed in classification tasks, describing also the limitations of the approach. Explain how the Gaussian and GMM assumptions can be employed in the Naive Bayes framework, and provide an expression for the class conditional likelihood in both cases.

**Theory - question 2 [8 points]**

Compare the empirical risk minimization formulation of Support Vector Machines and binary Logistic Regression.

- Provide the expression for the objective function and compare the loss terms of the two models.
- Provide the expression of the classification rule for both models, the interpretation of the classification score, and explain how the two models can be extended to perform non-linear classification.

**Theory - question 3 [4 points]**

Explain the role of accuracy in measuring the performance of classification systems.

Explain possible shortcomings of accuracy and how these shortcomings are addressed by the empirical Bayes risk framework, showing how the empirical Bayes risk accounts for both asymmetric error costs and possible mismatches between the application class prior probabilities and an evaluation set empirical class prior probabilities.

## Project part [max 8 points]

**Project - question 1 [5 points]**

Explain the performance on the project validation set of linear (Tied Gaussian, Logistic Regression, SVM) classifiers and quadratic (MVG, Logistic Regression, SVM) classifiers, explaining the characteristics of the dataset and of the classifiers that can justify:

- The improved performance of quadratic models.
- The similarities and/or differences in performance among linear models.
- The similarities and/or differences in performance among quadratic models.

The analysis should focus on minimum DCF costs (i.e. ignoring effects due to miscalibration). When relevant, consider, for each method, only the model that was selected as best candidate through cross-validation.

**Project - question 2 [3 points]**

Given the following functions (assume these functions are already implemented):

- `trainPCA`: trains a PCA model
- `applyPCA`: applies a PCA model to some data
- `trainLDA`: trains an LDA model
- `applyLDA`: applies an LDA model to some data
- `evaluateScores(S, L)`: computes a performance metric (e.g. minimum DCF for a predefined application) over the score array S with label vector L

a) Provide possible signatures (prototype) for the `trainPCA`, `applyPCA`, `trainLDA` and `applyLDA` functions, briefly explaining the function parameters and the return value.

b) Using these functions, write a short Python program to train and apply an LDA binary classifier. The classifier should simply output a classification score (i.e. you are not required to provide predicted labels or select a suitable score threshold). The performance of the classifier can be evaluated through the provided function `evaluateScores`.

Assume that you have at your disposal a training set, already divided into model training data (DTR, LTR), validation data (DVAL, LVAL), and an evaluation set (DTE, LTE). DTR, DVAL, and DTE are data matrices with samples organized as column vectors, whereas LTR, LVAL, and LTE are arrays containing the corresponding labels.

The program should employ in an appropriate way the provided data to train the classification model and to select an optimal value for the PCA dimensionality m.

The program should provide as output the classification scores and the corresponding performance for the evaluation set.
