# 01URTOV
# MACHINE LEARNING AND PATTERN RECOGNITION

## WRITTEN EXAM — Duration: 140 minutes

Theory part [max 24 points]

**Theory - question 1**

Describe the Principal Component Analysis (PCA) approach for dimensionality reduction, explaining:

- Goal and possible applications of the model
- Formulation of the model, including also the training objective and its solution
- The characteristics of the PCA components
- How the model can be employed in classification tasks

Describe the Linear Discriminant Analysis (LDA) approach for classification and dimensionality reduction, explaining:

- Goal and possible applications of the model
- Formulation of the model, including also the training objective and its solution, for both binary and multiclass tasks
- How the model can be employed in classification tasks

Compare the PCA and LDA approaches in the context of pattern classification

**Theory - question 2**

Describe Gaussian Mixture Models (GMM) in the context of density estimation and classification, covering the following aspects:

- Definition of the model density, explanation of the model parameters and interpretation of the GMM as a latent variable model
- Training procedure and estimation of the GMM parameters
- How the model can be employed for classification tasks, detailing both the training strategy and the inference procedure for both binary and multiclass problems, and explaining how the model can be used to address open-set scenarios. Relevant expressions must clearly reference the involved GMM densities and the corresponding parameters.

Explain the Naive Bayes assumption for generative models, and provide an explicit expression for a Naive Bayes GMM-based density model
```markdown

01URTOV
MACHINE LEARNING AND PATTERN RECOGNITION

WRITTEN EXAM — Duration: 140 minutes

Theory part [max 24 points]

**Theory - question 1**

Describe the Principal Component Analysis (PCA) approach for dimensionality reduction, explaining:

- Goal and possible applications of the model
- Formulation of the model, including also the training objective and its solution
- The characteristics of the PCA components
- How the model can be employed in classification tasks

Describe the Linear Discriminant Analysis (LDA) approach for classification and dimensionality reduction, explaining:

- Goal and possible applications of the model
- Formulation of the model, including also the training objective and its solution, for both binary and multiclass tasks
- How the model can be employed in classification tasks

Compare the PCA and LDA approaches in the context of pattern classification

**Theory - question 2**

Describe Gaussian Mixture Models (GMM) in the context of density estimation and classification, covering the following aspects:

- Definition of the model density, explanation of the model parameters and interpretation of the GMM as a latent variable model
- Training procedure and estimation of the GMM parameters
- How the model can be employed for classification tasks, detailing both the training strategy and the inference procedure for both binary and multiclass problems, and explaining how the model can be used to address open-set scenarios. Relevant expressions must clearly reference the involved GMM densities and the corresponding parameters.

Explain the Naive Bayes assumption for generative models, and provide an explicit expression for a Naive Bayes GMM-based density model
```

Project - question 1
Compare the relative performance on the project validation set of the Multivariate Gaussian
(MV, Tied MVG, and Gaussian Mixture models (GMIM) classifiers. Detail how the characteristics of the project data can explain their relative performance.

Project - question 2
are already implemented):
Consider a binary classification problem. Given the following functions (assume these functions
• trainSVM(D, L): the function trains a SVM classifier on the dataset matrix D (with samples organized as column vectors) with corresponding label array L. The function returns a Python object containing the trained model.
• scoreSV (model, D): the function computes an array of scores (organized as a row vector) for the samples in data matrix D for the model model returned by trainSV.
• trainLogReg(D, L, pi): the function trains a prior-weighted linear logistic regression model for an application with effective prior of pi for the True class. The function returns a tuple (w, b) containing the model weights w (column vector) and bias b (scalar). Assume that the corresponding scores can be interpreted as class posterior log-probability ratios.
• evaluate(S, L): the function computes an evaluation metric over the array of scores S, with associated array of labels L. The function assumes that S is an array of calibrated scores (for example, well calibrated log-likelihood ratios).
• split (D, L, p): the function splits a dataset in two parts. D is a matrix of samples (organized as column vectors), L is an array of associated labels (or None if labels are not available), and p is a value between 0.0 and 1.0. The function returns two partitions of the data ((D1, L1), (D2, L2)), with the first partition containing a fraction p of the original samples, and the second partition containing the remaining 1-p fraction of samples.
Using these functions, write a short Python program that trains a SVM classifier and a logistic regression classifier, and their logistic-regression-based score-level fusion. Assume that you have at your disposal a training set DTR, containing samples organized as column vectors, and an array LTR of corresponding labels. The program should properly organize the data as to compute the fusion model and provide an unbiased estimation of its performance. The computed model should then be applied and evaluated on an evaluation set DTE, with associated label array LTE. You can assume that:
•the SVM hyperparameters have been already optimized and their values are hard-coded inside the trainS function
• the logistic regression models do not require regularization
• the target application prior is = 0.1
• you are free to choose between single-fold protocols and K-fold protocols wherever needed For logistic regression models you can employ the target application prior, you are not requested to optimize the training prior hyperparameter.